---
description: Draft a complete role briefing for a Patrol seat (delegation-brief template)
argument-hint: <role> [task context]
---

Use the **patrol-briefing** skill to draft a full briefing for a seat.

- Role: **$1**
- Task context: $ARGUMENTS

Fill every section of the template (Context, Task, Why, Done looks like, Do NOT, Escalate if) with this role's specifics — no placeholders left unfilled. If `$1` is empty or not a recognised role, list the standard Patrol roles (orchestrator, executor, researcher) and ask which one before drafting.
